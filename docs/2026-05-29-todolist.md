# TODO / 下一步计划

## P0：下一轮优先修复

- [x] 重做设置页 UI 布局。
  - 设置页已改为左侧导航 + 右侧独立页面。
  - 自动化和应用场景已拆分成独立页面。
  - 主要控件宽度已放宽，减少文字压缩和按钮漏字。

- [x] 重构应用场景配置的产品逻辑。
  - 应用场景应定位成一种“应用颜色模式”，不是自动化附属项。
  - 规则命中后，将应用色覆盖到“固定颜色”和“单色呼吸”两类模式。
  - 当前优先级：基础设置 -> 时间计划 -> 应用场景 -> 空闲降亮。

- [x] 修复应用场景规则没有即时生效的问题。
  - 当前测试现象：Spotify 成为活动窗口后，键盘没有切换到规则颜色。
  - 已改为由托盘进程检测前台应用，并写入 `foreground-app.json`。
  - 服务监听该状态文件变化并重建运行态灯效。
  - 托盘会周期刷新前台应用状态，避免服务读到过期状态。

- [x] 修复应用场景自动图标色保存逻辑。
  - 自动图标取色开启时，不再用隐藏的手动颜色覆盖图标色。
  - 规则命中后，用图标色或手动色生成固定颜色 / 单色呼吸效果。

## P1：下一轮可开始设计 / 部分实现

- [x] 敲字闪烁。
  - 目标：基础亮度 50 或 75，键盘输入后短时提升到 100，再平滑回落。
  - 已由托盘进程监听键盘输入，写入 `typing-pulse.json`。
  - 服务读取轻量状态文件并在当前灯效帧上临时抬高亮度，不改主配置。

- [x] 音乐模式优化。
  - 增加“亮度脉冲”模式：固定颜色，按音乐电平控制亮度，不变色。
  - 优化当前“按电平变色”：加入平滑、阈值和冷却时间，避免颜色跳变过快。
  - 已加入模式拆分、噪声门、节拍阈值和峰值保持。
  - UI 已改为预设优先，内置流行、摇滚、经典、电子。
  - 高级参数折叠显示，可保存最多 8 个命名自定义预设。
  - 中长期方案：FFT 低频鼓点检测，减少全频电平乱跳。

- [x] 诊断页。
  - 显示服务状态、驱动 DLL 状态、当前前台应用、当前命中的应用场景、更新检查状态和配置目录。
  - 放在设置页独立“诊断”页，便于测试机排错。

## P1：测试机重点验证

- [ ] 设置页是否仍有漏字、按钮异常或横向滚动。
- [ ] Spotify 应用场景在窗口激活后是否正确切换颜色。
- [ ] 敲字闪烁在固定颜色、单色呼吸、RGB 循环、音乐模式下是否有明显延迟或过亮。
- [ ] 音乐模式“按电平变色”和“仅亮度脉冲”是否更连贯，噪声门和节拍阈值默认值是否合适。
- [ ] 音乐预设选择、保存自定义、覆盖自定义、删除自定义是否正常。

- [ ] 应用图标取色策略优化。
  - 当前可先用图标主色。
  - 后续可以增加“自动色预览”和“手动覆盖”。

## P2：后续功能

- [ ] 专辑封面自动取色。
  - 优先适配 Spotify。
  - 作为音乐模式亮度脉冲的基础色来源。

- [ ] 设置导入 / 导出。
  - 方便测试机和主力机同步规则。

- [ ] 日志与诊断增强。
  - 显示最近服务错误、最近驱动调用异常和一键导出诊断包。

## 暂停 / 不进入主线

- [ ] 分区灯效。
  - 测试结论：当前键盘不支持分区。
  - 三区音乐律动、三区跑马/波浪暂停开发。
  - 分区控制测试保留在“实验性功能”中，仅用于验证其它机器。
